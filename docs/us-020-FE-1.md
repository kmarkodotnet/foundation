# us-020-FE-1 | FE: Submission Step Form

**Story:** US-020 | [Beadás] Beadás adatainak rögzítése  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** M  
**Sprint:** Sprint 3  
**Depends on:** us-020-BE-1.md, us-010-FE-1.md  
**Blocks:** us-021-FE-1.md

---

## Story

> As a **pályázati munkatárs**, I want to record the submission details so that the submission fact and date are documented.

---

## Scope

Submission step component with form for submission data, "Send for approval" button.

---

## Implementation Checklist

### Feature Module
- [ ] `detail/workflow/step-submission/step-submission.component.ts` + `.html` + `.scss`
  - Beadás időpontja (DatePicker, kötelező)
  - Beadás módja (MatSelect, kódszótár)
  - Azonosító (string, opcionális)
  - Megjegyzés (textarea, opcionális)
  - „Mentés" gomb
  - „Jóváhagyásra küldés" gomb (PalyazatiMunkatars, ha van adat)

### Service
- [ ] `WorkflowService.updateSubmissionStep(appId: string, data: UpdateSubmissionRequest): Observable<WorkflowStepDetailDto>` — PUT /api/v1/applications/:id/workflow/submission
- [ ] `WorkflowService.requestApproval(appId: string, stepType: string): Observable<void>` — POST `/api/v1/applications/:id/workflow/${stepType}/request-approval` (generikus; stepType pl. `'submission'`, `'budget-plan'`, `'settlement'`)

### Reactive Form
- [ ] `submittedAt`: `[null, Validators.required]`
- [ ] `submissionMethodId`: `[null]`
- [ ] `externalIdentifier`: `['']`
- [ ] `notes`: `['']`

### Authorization
- [ ] „Mentés"/„Jóváhagyásra küldés": `*hasRole="['Admin', 'PalyazatiMunkatars']"`
- [ ] LOCKED állapotban disabled

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `PUT` | `/api/v1/applications/:id/workflow/submission` | `UpdateSubmissionRequest` | `WorkflowStepDetailDto` |
| `POST` | `/api/v1/applications/:id/workflow/${stepType}/request-approval` | — | 204 |

---

## UX / Behaviour Specification

| Scenario | Expected behaviour |
|---|---|
| Sikeres mentés | Toast: „Beadás adatai elmentve." |
| „Jóváhagyásra küldés" | POST hívás, toast: „Jóváhagyási kérés elküldve az Elnöknek." |
| Kötelező mező hiányzik | Validáció error |

---

## Required Tests

- [ ] `StepSubmissionComponent` — form invalid ha nincs submittedAt
- [ ] `StepSubmissionComponent` — mentés hívja `WorkflowService.updateSubmissionStep()`
- [ ] `StepSubmissionComponent` — „Jóváhagyásra küldés" hívja `requestApproval()`
- [ ] `WorkflowService.updateSubmissionStep()` — PUT hívás helyes payload-dal

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: [2. Beadás] lépés ACTIVE állapotban szerkeszthető.
- [ ] AC2: Kötelező: beadás időpontja, beadás módja.
- [ ] AC3: Lépés lezárásakor pályázat SUBMITTED.
- [ ] AC4: Jóváhagyásra küldés gomb, Elnök értesítést kap.
- [ ] AC5: Opcionális mezők menthetők.
