# us-030-FE-1 | FE: Record Result Step (Won)

**Story:** US-030 | [Eredmény] Nyert eredmény rögzítése  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** M  
**Sprint:** Sprint 3  
**Depends on:** us-030-BE-1.md, us-010-FE-1.md  
**Blocks:** us-031-FE-1.md

---

## Story

> As a **pályázati munkatárs**, I want to record that we won the grant and the awarded amount.

---

## Scope

Result step component with Won/Lost radio selection; if Won, shows amount and date fields. Shares component with us-031 (Not Won path).

---

## Implementation Checklist

### Feature Module
- [ ] `detail/workflow/step-result/step-result.component.ts` + `.html` + `.scss`
  - Nyert / Nem nyert radio group (MatRadioGroup)
  - Ha Nyert: elnyert összeg (kötelező, currency input), eredmény dátuma (kötelező, DatePicker), azonosító (opcionális)
  - Megerősítő dialog (us-031 Nem nyert ághoz)
  - „Mentés" gomb

### Service
- [ ] `WorkflowService.recordResult(appId: string, data: RecordResultRequest): Observable<ApplicationDto>` — PUT /api/v1/applications/:id/workflow/result

### Reactive Form
- [ ] `isWon`: `[null, Validators.required]` (radio)
- [ ] `awardedAmount`: kondicionálisan kötelező ha isWon=true, `Validators.min(1)`
- [ ] `resultDate`: kondicionálisan kötelező ha isWon=true
- [ ] `resultIdentifier`: `['']` (opcionális, max 200 karakter)

### Authorization
- [ ] Szerkesztés: `*hasRole="['Admin', 'PalyazatiMunkatars']"`

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `PUT` | `/api/v1/applications/:id/workflow/result` | `RecordResultRequest` | `ApplicationDto` |

---

## UX / Behaviour Specification

| Scenario | Expected behaviour |
|---|---|
| „Nyert" kiválasztva | Összeg és dátum mezők megjelennek (kötelező) |
| „Nem nyert" kiválasztva | Megerősítő dialog nyílik (us-031 path) |
| Sikeres mentés (nyert) | Toast: „Nyert eredmény rögzítve. A pályázat WON státuszba került.", lépések [4]–[9] megjelennek aktívan |

---

## Required Tests

- [ ] `StepResultComponent` — „Nyert" választva → összeg és dátum mezők láthatók, kötelezők
- [ ] `StepResultComponent` — összeg nélkül submit disabled
- [ ] `StepResultComponent` — sikeres mentés → `WorkflowService.recordResult()` hívódik
- [ ] `StepResultComponent` — WON után lépések [4]–[9] megjelennek

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: [3. Eredmény] lépésen Nyert/Nem nyert választható.
- [ ] AC2: Nyert: összeg és dátum kötelező.
- [ ] AC3: Nyert → WON, lépések [4]–[9] ACTIVE-ra váltanak.
- [ ] AC4: Lépés COMPLETED.
