# us-060-FE-1 | FE: Invoice Step Form

**Story:** US-060 | [Számla] Új számla rögzítése  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** M  
**Sprint:** Sprint 4  
**Depends on:** us-060-BE-1.md, us-010-FE-1.md  
**Blocks:** us-061-FE-1.md, us-062-FE-1.md, us-063-FE-1.md

---

## Story

> As a **pénzügyes**, I want to record a new invoice so that the financial performance is documented.

---

## Scope

Invoices step component with invoice list, add invoice form, conditional payment date field, and financial summary panel.

---

## Implementation Checklist

### Feature Module
- [ ] `detail/workflow/step-invoices/step-invoices.component.ts` + `.html` + `.scss`
  - Pénzügyi összesítő panel (US-063 scope: elnyert, tervezett, rögzített, fizetve, fizetetlen, egyenleg)
  - Számlák lista (MatTable, szűrhető: fizetve/nem fizetve, rendezhető: dátum, összeg)
  - „Új számla" gomb → `InvoiceFormComponent`

- [ ] `InvoiceFormComponent` (mat-dialog vagy expansion panel):
  - Szállító neve (MatInput, required)
  - Számla sorszáma (MatInput, required)
  - Kiállítás dátuma (DatePicker, required)
  - Összeg (number input, required)
  - Fizetve (MatCheckbox)
  - Fizetés időpontja (DatePicker, megjelenik ha fizetve=true, required)
  - Kapcsolt alvállalkozói szerződés (MatSelect, optional)
  - Kapcsolt költési terv tétel (MatSelect, optional)
  - Megjegyzés (textarea, optional)
  - Fizetési dátum < kiállítás dátuma → sárga figyelmeztetés (non-blocking)

### Service
- [ ] `InvoiceService.getInvoices(appId: string): Observable<InvoiceListDto>` — GET /api/v1/applications/:id/invoices
- [ ] `InvoiceService.createInvoice(appId: string, data: CreateInvoiceRequest): Observable<InvoiceDto>` — POST /api/v1/applications/:id/invoices
- [ ] `InvoiceListDto` és `InvoiceSummaryDto` TypeScript interfészek (kanonikus definíció — us-063 erre hivatkozzon):
  ```ts
  interface InvoiceListDto {
    summary: InvoiceSummaryDto;
    items: InvoiceDto[];
  }
  interface InvoiceSummaryDto {
    awardedAmount: number;
    totalPlanned: number;
    totalInvoiced: number;
    totalPaid: number;
    totalUnpaid: number;
    balance: number; // awardedAmount - totalInvoiced
  }
  ```

### Reactive Form
- [ ] `supplierName`: `['', Validators.required]`
- [ ] `invoiceNumber`: `['', Validators.required]`
- [ ] `issueDate`: `[null, Validators.required]`
- [ ] `amount`: `[null, [Validators.required, Validators.min(1)]]`
- [ ] `isPaid`: `[false]`
- [ ] `paymentDate`: kondicionálisan kötelező ha `isPaid=true`
- [ ] `vendorContractId`: `[null]`
- [ ] `budgetItemId`: `[null]`
- [ ] `notes`: `['']`

### Authorization
- [ ] „Új számla": `*hasRole="['Admin', 'Penzugyes']"`

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `GET` | `/api/v1/applications/:id/invoices` | — | `InvoiceListDto` |
| `POST` | `/api/v1/applications/:id/invoices` | `CreateInvoiceRequest` | `InvoiceDto` |

---

## UX / Behaviour Specification

| Scenario | Expected behaviour |
|---|---|
| „Fizetve" bekapcsolva | Fizetés időpontja kötelező mező megjelenik |
| Fizetési dátum < kiállítás | Sárga figyelmeztetés, nem blokkoló |
| Sikeres mentés | Toast „Számla rögzítve.", lista frissül |
| Összesítő | Piros ha számlák összege > elnyert összeg |

---

## Required Tests

- [ ] `InvoiceFormComponent` — isPaid=true → paymentDate kötelező
- [ ] `InvoiceFormComponent` — paymentDate < issueDate → figyelmeztetés jelenik meg
- [ ] `StepInvoicesComponent` — mentés hívja `InvoiceService.createInvoice()`
- [ ] `InvoiceService.createInvoice()` — POST /invoices helyes payload-dal

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: [7. Számlák] lépés WON pályázatnál elérhető.
- [ ] AC2: Ha fizetve=igen, fizetési dátum kötelező.
- [ ] AC3: Fizetési dátum < kiállítás dátuma → nem blokkoló figyelmeztetés.
- [ ] AC4: Összesítő panel megjelenik.
