# us-070-FE-1 | FE: Settlement Step Form

**Story:** US-070 | [Elszámolás] Elszámolás rögzítése  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** M  
**Sprint:** Sprint 5  
**Depends on:** us-070-BE-1.md, us-010-FE-1.md  
**Blocks:** us-071-FE-1.md

---

## Story

> As a **pénzügyes**, I want to record the settlement so that the financial report is documented.

---

## Scope

Settlement step component with a form for settlement data, low-coverage warning, and "Send for approval" button triggering Elnök notification.

---

## Implementation Checklist

### Feature Module
- [ ] `detail/workflow/step-settlement/step-settlement.component.ts` + `.html` + `.scss`
  - Elszámolás időpontja (DatePicker, required)
  - Elszámolás módja (MatSelect, kódszótár, optional)
  - Összefoglaló leírás (textarea, optional)
  - Megjegyzés (textarea, optional)
  - Alacsony fedezettség figyelmeztetés (sárga banner ha hasLowCoverageWarning=true):
    „A rögzített számlák összege X%, ami nem éri el az elnyert összeg 80%-át. Biztosan folytatod?"
  - „Mentés" gomb
  - „Jóváhagyásra küldés" gomb (PalyazatiMunkatars/Penzugyes, `*hasRole`)

### Service
- [ ] `SettlementService.getSettlement(appId: string): Observable<SettlementDto>` — GET /api/v1/applications/:id/settlement
- [ ] `SettlementService.saveSettlement(appId: string, data: RecordSettlementRequest): Observable<SettlementDto>` — PUT /api/v1/applications/:id/settlement
- [ ] `WorkflowService.requestApproval(appId: string, stepType: 'settlement'): Observable<void>` — POST /api/v1/applications/:id/workflow/settlement/request-approval

### Reactive Form
- [ ] `settlementDate`: `[null, Validators.required]`
- [ ] `settlementMethodId`: `[null]`
- [ ] `description`: `['']`
- [ ] `notes`: `['']`

### Authorization
- [ ] „Mentés": `*hasRole="['Admin', 'Penzugyes']"`
- [ ] „Jóváhagyásra küldés": `*hasRole="['Admin', 'Penzugyes']"`

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `GET` | `/api/v1/applications/:id/settlement` | — | `SettlementDto` |
| `PUT` | `/api/v1/applications/:id/settlement` | `RecordSettlementRequest` | `SettlementDto` |
| `POST` | `/api/v1/applications/:id/workflow/settlement/request-approval` | — | 204 |

---

## UX / Behaviour Specification

| Scenario | Expected behaviour |
|---|---|
| hasLowCoverageWarning=true | Sárga banner jelenik meg %-os értékkel |
| „Jóváhagyásra küldés" | POST request-approval, toast „Jóváhagyási kérés elküldve az Elnöknek." |
| Sikeres mentés | Toast „Elszámolás adatai elmentve." |

---

## Required Tests

- [ ] `StepSettlementComponent` — form invalid ha nincs settlementDate
- [ ] `StepSettlementComponent` — hasLowCoverageWarning=true → sárga banner látható
- [ ] `StepSettlementComponent` — mentés → `SettlementService.saveSettlement()` hívódik
- [ ] `StepSettlementComponent` — „Jóváhagyásra küldés" → `WorkflowService.requestApproval()` hívódik

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: [9. Elszámolás] lépés WON pályázatnál elérhető.
- [ ] AC2: Elszámolás időpontja kötelező.
- [ ] AC3: Alacsony fedezettség esetén figyelmeztető banner (nem blokkoló).
- [ ] AC4: Jóváhagyásra küldés után Elnök értesítést kap.
