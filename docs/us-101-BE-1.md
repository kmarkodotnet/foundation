# us-101-BE-1 | BE: Edit Granter

**Story:** US-101 | [Pályáztató] Pályáztató adatainak szerkesztése  
**Layer:** Backend  
**Priority:** Közepes  
**Size estimate:** XS  
**Sprint:** Sprint 3  
**Depends on:** us-100-BE-1.md  
**Blocks:** —

---

## Story

> As a **pályázati munkatárs**, I want to update a granter's data so that contact information stays current.

---

## Scope

Updates all editable fields of a Granter entity.

---

## Implementation Checklist

### Application Layer
- [ ] Command: `Granters/Commands/UpdateGranter/UpdateGranterCommand.cs` — Guid GranterId, string Name, string? Description, string? Phone, string? Email
- [ ] Handler: loads Granter, updates fields, checks name uniqueness (excluding self)
- [ ] Validator: same as CreateGranterCommandValidator

### API Layer
- [ ] `PUT /api/v1/granters/{id}` — `[RequireRole(Admin, PalyazatiMunkatars)]`
  - Returns `200 OK` with `GranterDto`

### Authorization
- [ ] Admin, PalyazatiMunkatars

---

## API Contract

```
PUT /api/v1/granters/{id}
```

Same request body as create. Returns `GranterDto`.

| Response | Description |
|---|---|
| **200** | Updated GranterDto |
| **422** | Name already taken by another granter |

---

## MediatR Pipeline Notes
- `AuditBehaviour`: Granter field updates

---

## Required Tests

### Unit Tests
- [ ] `UpdateGranterCommandHandlerTests` — fields updated
- [ ] `UpdateGranterCommandValidatorTests` — name taken by other → error

### Integration Tests
- [ ] `GranterEndpointTests` — PUT → 200 updated

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: Szerkesztés gomb a részletes nézetben.
- [ ] AC2: Minden mező szerkeszthető.
- [ ] AC3: Módosítás naplózódik.
