# us-101-FE-1 | FE: Edit Granter

**Story:** US-101 | [Pályáztató] Pályáztató adatainak szerkesztése  
**Layer:** Frontend  
**Priority:** Közepes  
**Size estimate:** XS  
**Sprint:** Sprint 3  
**Depends on:** us-101-BE-1.md, us-103-FE-1.md  
**Blocks:** —

---

## Story

> As a **pályázati munkatárs**, I want to update a granter's details.

---

## Scope

Edit button on the Granter detail page. Reuses `GranterFormComponent` in edit mode.

---

## Implementation Checklist

### Feature Module
- [ ] `GranterDetailComponent` — „Szerkesztés" gomb (`*hasRole="['Admin', 'PalyazatiMunkatars']"`)
  - Megnyitja `GranterFormComponent` előre kitöltve

### Service
- [ ] `GranterService.updateGranter(id: string, data: UpdateGranterRequest): Observable<GranterDto>` — PUT /api/v1/granters/:id

### State / Data Flow
- [ ] Sikeres szerkesztés: Toast „Pályáztató adatai frissítve.", detail oldal frissül

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `PUT` | `/api/v1/granters/:id` | `UpdateGranterRequest` | `GranterDto` |

---

## Required Tests

- [ ] `GranterDetailComponent` — „Szerkesztés" gomb → form előre kitöltve
- [ ] `GranterFormComponent` (edit mode) — mentés → `GranterService.updateGranter()` hívódik

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: Szerkesztés gomb a pályáztató részletes nézetén.
- [ ] AC2: Minden mező szerkeszthető.
- [ ] AC3: Módosítás naplózódik.
