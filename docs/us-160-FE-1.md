# us-160-FE-1 | FE: Users List

**Story:** US-160 | [Admin] Felhasználók listázása és áttekintése  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** S  
**Sprint:** Sprint 2  
**Depends on:** us-160-BE-1.md  
**Blocks:** us-161-FE-1.md, us-162-FE-1.md

---

## Story

> As an **adminisztrátor**, I want to view all users with their roles and status.

---

## Scope

Users admin page with filterable table. Inactive users shown greyed out.

---

## Implementation Checklist

### Feature Module
- [ ] `src/app/features/admin/users/users.module.ts` (lazy-loaded, Admin only)
- [ ] `UsersPageComponent` — `/admin/users` route:
  - Szűrő: névkereső (MatInput), szerepkör (MatSelect), státusz toggle
  - MatTable: avatar, teljes név, e-mail, szerepkör badge, státusz, utolsó belépés
  - Inaktív felhasználók szürkítve
  - Soronként: „Szerepkör módosítása" (US-161), „Inaktiválás/Reaktiválás" (US-162)

### Service
- [ ] `UserService.getUsers(filters: UserFilters): Observable<PagedResult<UserListItemDto>>` — GET /api/v1/users

### Authorization
- [ ] Route guard: `hasRole(['Admin'])`

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `GET` | `/api/v1/users?search=&role=` | — | `PagedResult<UserListItemDto>` |

---

## Required Tests

- [ ] `UsersPageComponent` — lista betöltéskor `UserService.getUsers()` hívódik
- [ ] `UsersPageComponent` — inaktív felhasználó szürkítve jelenik meg
- [ ] `UsersPageComponent` — szűrő változásra API újrahívódik

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: Felhasználók oldal Admin menüből.
- [ ] AC2: Lista: profilkép, név, e-mail, szerepkör, státusz, utolsó belépés.
- [ ] AC3: Szűrés: névre, e-mailre, szerepkörre.
- [ ] AC4: Aktív/inaktív vizuálisan megkülönböztetve.
