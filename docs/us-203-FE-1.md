# us-203-FE-1 | FE: Platform-szintű felhasználó meghívása UI

**Story:** US-203 | [Platform (CR2)] Platform-szintű felhasználó meghívása  
**Layer:** Frontend  
**Priority:** Magas  
**Size estimate:** S  
**Sprint:** CR2-Sprint C  
**Depends on:** us-203-BE-1.md  
**Blocks:** —

---

## Scope

A `/platform/users` lista és az „Új meghívó" dialog megvalósítása platform-scope-ra.

---

## Implementation Checklist

### Feature Module (`src/app/features/platform/users/`)
- [ ] `platform-users-list.component.ts` + `.html` — platform scope-ú felhasználók listája
- [ ] `platform-invite-dialog.component.ts` + `.html` — form: email, szerepkör (`PlatformAdmin` / `PlatformAuditor`)
- [ ] `PlatformUserService`: `list()`, `invite()` metódusok
- [ ] Route: `platform/users`

### Reactive Form
- [ ] `email`: `[required, email]`
- [ ] `role`: `[required]`, MatSelect: `PlatformAdmin` / `PlatformAuditor`

### State / Data Flow
- [ ] Sikeres meghívás → snackbar „Meghívó elküldve." + lista frissítés (PENDING meghívók látszanak)
- [ ] 409 hiba → mező-szintű hibaüzenet: „Ez az e-mail cím már egy Owner-hez/Foundation-hoz tartozik."

---

## API Consumed

| Method | URL | Body | Response |
|---|---|---|---|
| `GET` | `/api/v1/platform/users` | — | `PlatformUserListItem[]` |
| `POST` | `/api/v1/platform/users/invite` | `{ email, role }` | `201` |

---

## Required Tests

- [ ] `PlatformInviteDialogComponent` — submit disabled üres e-mail esetén
- [ ] `PlatformInviteDialogComponent` — sikeres meghívás → dialog bezárul, snackbar megjelenik
- [ ] `PlatformUserService.invite()` → POST helyes payload-dal

**Coverage:** ≥80%.
