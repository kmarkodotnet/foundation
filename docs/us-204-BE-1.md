# us-204-BE-1 | BE: Platform-szintű technikai beállítások

**Story:** US-204 | [Platform (CR2)] Platform-szintű technikai beállítások  
**Layer:** Backend  
**Priority:** Közepes  
**Size estimate:** S  
**Sprint:** CR2-Sprint C  
**Depends on:** us-200-BE-1.md  
**Blocks:** us-204-FE-1.md

---

## Scope

Platform-szintű konfigurációs értékek (fájlméret-korlát, meghívó érvényességi idő, értesítési határidők, default kódszótár-sablon azonosítója) olvasása és frissítése. Az értékek egy `PlatformSettings` táblában tárolódnak (singleton sor).

---

## Implementation Checklist

### Domain Layer (`GrantManagement.Domain`)
- [ ] `PlatformSettings` aggregátum (singleton — lásd `domain-model.md` CR2.2.6): `Domain/Tenancy/PlatformSettings.cs`
  - Mezők: `Id` (sentinel `00000000-0000-0000-0000-000000000001`), `MaxFileSizeMb`, `InvitationValidityHours`, `NotificationLeadDays`, `DefaultOwnerCodeListTemplateId`, `UpdatedAt`, `UpdatedByUserId`
  - Domain művelet: `PlatformSettings.Update(maxFileSizeMb, invitationValidityHours, notificationLeadDays, defaultTemplateId, updatedBy)` invariáns-ellenőrzéssel (`[1, 500]`, `[1, 720]`, `[1, 90]`)
  - Domain event: `PlatformSettingsUpdated`

### Infrastructure Layer
- [ ] `PlatformSettingsConfiguration.cs` + DbSet
- [ ] Migráció + seed: sentinel rekord létrehozása default értékekkel (MaxFileSizeMb=50, InvitationValidityHours=72, NotificationLeadDays=7, DefaultOwnerCodeListTemplateId=null)

### Application Layer (`GrantManagement.Application`)
- [ ] `GetPlatformSettingsQuery` + Handler
- [ ] `UpdatePlatformSettingsCommand` + Handler: hívja `platformSettings.Update(...)`, a domain event handler készít audit-bejegyzést `PLATFORM_SETTINGS_UPDATED` művelettípussal (lásd us-233-BE-1)
- [ ] `PlatformSettingsResponse` DTO, `UpdatePlatformSettingsRequest` DTO

### Snapshot-mező garancia (AC3) — implementációs követelmény
- [ ] Az `Owner.Provision()` (us-200-BE-1) és `Foundation.Create()` (us-210-BE-1) handlerek a provisioningkor **lemásolják** a `PlatformSettings` aktuális mezőit a saját `OwnerSettings` és `FoundationSettings` mezőikbe (lásd `domain-model.md` CR2.3 snapshot beállítások).
- [ ] Az `UpdatePlatformSettingsCommandHandler` **nem érinti** a meglévő `Owners` és `Foundations` rekordok beállításait — kizárólag a `PlatformSettings` aggregátumot frissíti.
- [ ] Integration teszt (AC3): meglévő Owner snapshot beállítása `MaxFileSizeMb = 50` → `PATCH /platform/settings { maxFileSizeMb: 100 }` → meglévő Owner snapshot változatlan (50), új Owner provisioning már 100 értékkel.

### API Layer (`GrantManagement.API`)
- [ ] `PlatformSettingsController`:
  - `GET /api/v1/platform/settings` — `[Authorize(Policy = "CanReadPlatform")]`
  - `PATCH /api/v1/platform/settings` — `[Authorize(Policy = "IsPlatformAdmin")]`

---

## API Contract

### PATCH /api/v1/platform/settings
**Auth:** `IsPlatformAdmin`

**Request:**
```json
{
  "maxFileSizeMb": 100,
  "invitationExpiryHours": 48
}
```

**200:** frissített `PlatformSettingsResponse`  
**400** — Érvénytelen értékek (pl. MaxFileSizeMb < 1)

---

## FluentValidation Rules

| Field | Rule |
|---|---|
| `MaxFileSizeMb` | `GreaterThan(0).LessThanOrEqualTo(500)` |
| `InvitationExpiryHours` | `GreaterThan(0).LessThanOrEqualTo(8760)` |
| `DefaultDeadlineNotificationDays` | `GreaterThan(0).LessThanOrEqualTo(365)` |

---

## Required Tests

### Unit Tests
- [ ] `UpdatePlatformSettingsCommandHandlerTests` — sikeres frissítés + audit
- [ ] Validáció: MaxFileSizeMb = 0 → hiba

### Integration Tests
- [ ] `PATCH /api/v1/platform/settings` → 200, értékek frissülnek
- [ ] `GET /api/v1/platform/settings` → jelenleg érvényes értékek

**Coverage:** ≥80%.

---

## Acceptance Criteria

- [ ] AC1: Beállítható: fájlméret-korlát, meghívó érvényessége, értesítési határidők, default kódszótár-sablon.
- [ ] AC2: Módosítás azonnal érvénybe lép új Owner/Foundation provisioningnál.
- [ ] AC3: Meglévő Owner/Foundation beállításait nem írja felül automatikusan.
- [ ] AC4: Módosítás audit-bejegyzéssel.
