# us-204-FE-1 | FE: Platform technikai beállítások UI

**Story:** US-204 | [Platform (CR2)] Platform-szintű technikai beállítások  
**Layer:** Frontend  
**Priority:** Közepes  
**Size estimate:** S  
**Sprint:** CR2-Sprint C  
**Depends on:** us-204-BE-1.md  
**Blocks:** —

---

## Scope

A `/platform/settings` oldal: platform-szintű beállítások megjelenítése és szerkesztése egyszerű PATCH-alapú formával.

---

## Implementation Checklist

### Feature Module (`src/app/features/platform/settings/`)
- [ ] `platform-settings.component.ts` + `.html`:
  - Szerkeszthető mezők: MaxFileSizeMb, InvitationExpiryHours, DefaultDeadlineNotificationDays
  - Default kódszótár-sablon MatSelect (Owner sablon-lista betöltve)
  - PlatformAuditor számára readonly nézet, „Szerkesztés" gomb nélkül
  - Mentés gomb → PATCH
- [ ] `PlatformSettingsService`: `get()`, `update()` metódusok
- [ ] Route: `platform/settings`

---

## API Consumed

| Method | URL |
|---|---|
| `GET` | `/api/v1/platform/settings` |
| `PATCH` | `/api/v1/platform/settings` |

---

## UX / Behaviour

| Scenario | Expected behaviour |
|---|---|
| PlatformAuditor | Readonly nézet, szerkesztés nem elérhető |
| Sikeres mentés | Snackbar „Beállítások mentve." |
| Érvénytelen érték | Mező-szintű hibaüzenet |

---

## Required Tests

- [ ] `PlatformSettingsComponent` — PlatformAuditor → mezők readonly
- [ ] `PlatformSettingsComponent` — sikeres mentés → snackbar
- [ ] `PlatformSettingsService.update()` → PATCH helyes payload-dal

**Coverage:** ≥80%.
