# us-215-FE-1 | FE: Cross-foundation dashboard UI

**Story:** US-215 | [Owner (CR2)] Cross-foundation dashboard  
**Layer:** Frontend  
**Priority:** Közepes  
**Size estimate:** L  
**Sprint:** CR2-Sprint D  
**Depends on:** us-215-BE-1.md, us-220-FE-1.md  
**Blocks:** —

---

## Scope

Az `/owner/dashboard` oldal megvalósítása: alapítványonkénti statisztika kártyák, összevont fejléc számok és Foundation-switcher integráció (kattintásra scope-váltás).

---

## Implementation Checklist

### Feature Module (`src/app/features/owner/dashboard/`)
- [ ] `owner-dashboard.component.ts` + `.html`:
  - Fejléc összesítő: össznyert összeg, elszámolatlan keret (nagy számok HUF formátumban)
  - Foundation kártyák: név, státusz-rozéta (InProgress/Won/Lost/Submitted), WonAmount
  - Minden kártyán „Belépés" gomb → foundation switcher hívás (`ScopeService.switchTo(foundationId)`)
  - `MatProgressSpinnerModule` betöltés közben
- [ ] `OwnerDashboardService.getDashboard()` metódus
- [ ] Pénznem formátor pipe: `{{ amount | currency:'HUF':'symbol':'0.0-0' }}`
- [ ] Route: `owner/dashboard`

### State / Data Flow
- [ ] Dashboard adatai on-init betöltve
- [ ] Foundation kártyára kattintás → `ScopeService.switchTo(foundationId)` → navigálás `/app/applications`-re az új JWT-vel
- [ ] Loading state spinner
- [ ] Error state: toast + retry gomb

---

## API Consumed

| Method | URL | Response |
|---|---|---|
| `GET` | `/api/v1/owner/reports/dashboard` | `OwnerDashboardResponse` |

---

## UX / Behaviour

| Scenario | Expected behaviour |
|---|---|
| Foundation „Belépés" kattintás | Scope-switch → navigálás az adott Foundation-ba |
| Üres Foundation (0 pályázat) | Kártya megjelenik 0 értékekkel |
| Betöltés | Spinner, adatok helyett skeleton |

---

## Required Tests

- [ ] `OwnerDashboardComponent` — betöltés után Foundation kártyák megjelennek
- [ ] `OwnerDashboardComponent` — „Belépés" gomb scope-switcht hív és navigál
- [ ] `OwnerDashboardService.getDashboard()` → GET helyes URL-re

**Coverage:** ≥80%.
